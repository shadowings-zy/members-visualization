# Python 依赖包
# 用于数据拉取脚本 scripts/fetch-members.py

# HTTP 请求库 - 用于调用 GitHub API
requests>=2.31.0

# 环境变量管理 - 用于加载 .env 文件
python-dotenv>=1.0.0

# 注意：这些依赖包在 GitHub Actions 中会自动安装
# 本地开发时可以运行：pip install -r requirements.txt
