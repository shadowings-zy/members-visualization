# 设置 npm 镜像源（可选，根据需要调整）
# registry=https://registry.npmmirror.com/

# 自动安装 peer dependencies
auto-install-peers=true

# 保存精确版本号
save-exact=true

# 设置日志级别
loglevel=warn

# 禁用更新检查
update-notifier=false

# 设置缓存目录
cache=.npm

# 设置进度条
progress=true
